import React from "react";

const Header = () => {
  return (
    <header>
      {/* Add header content */}
      <nav>{/* Add navigation links */}</nav>
    </header>
  );
};

export default Header;
